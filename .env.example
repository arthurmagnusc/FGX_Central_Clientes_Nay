# ---- modo demo (Hono / Vercel api) — ativo hoje ----
ADMIN_SENHA_INICIAL=fgxadmin2026
DEMO_CLIENT_SENHA=fiedra2026
WEBHOOK_AVALIACAO_AJUSTE=
SESSION_SECRET=change-me-in-production
COOKIE_SECURE=false
FGX_MEMORY_DB=0

# ---- go-live Supabase (front — público no bundle) ----
# Preencher quando o projeto Supabase existir. Enquanto vazias, o app usa a API Hono.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_FUNCTIONS_URL=

# ---- Edge Functions (segredo — supabase secrets set --env-file .env.functions) ----
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_JWT_SECRET=
# ORIGENS_PERMITIDAS=https://portal.fgxgestao.com,http://localhost:5173
